name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    uses: jh1nresh/.github/.github/workflows/web-ci.yml@0cb6fd5b730ced897c767d1c9979aba4e49d201d
    with:
      node-version: "22"
      verify-script: check
      output-directory: .next
      audit-production: false
      timeout-minutes: 20
      job-key: maiat-agent-spend-landing
